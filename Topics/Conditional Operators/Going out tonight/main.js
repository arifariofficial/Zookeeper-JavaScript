// Setting the age
var age = 18;

// Checking the condition
var condition =
  age < 18
    ? "Sorry, but you are a minor."
    : "That's ok. You can enter the pub.";

// Printing the result
console.log(condition);
